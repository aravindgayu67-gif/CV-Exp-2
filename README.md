# CV-Exp-2
This project demonstrates Gaussian Image Pyramid generation using OpenCV in Python. It progressively downsamples an image into multiple levels and applies edge detection (Canny) at each level to observe how details change with resolution. Useful for understanding multi-scale image processing in computer vision.
